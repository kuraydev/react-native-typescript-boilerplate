/**
 * Local storage service stub.
 *
 * This is an intentional placeholder so the documented `@local-storage` path
 * alias resolves out of the box. Wire up your persistence layer of choice here
 * (e.g. `@react-native-async-storage/async-storage` or `react-native-mmkv`).
 *
 * @example
 * import AsyncStorage from "@react-native-async-storage/async-storage";
 *
 * export const setItem = (key: string, value: string) =>
 *   AsyncStorage.setItem(key, value);
 *
 * export const getItem = (key: string) => AsyncStorage.getItem(key);
 */

export {};
