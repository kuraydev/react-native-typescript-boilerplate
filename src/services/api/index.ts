/**
 * API service stub.
 *
 * This is an intentional placeholder so the documented `@api` path alias
 * resolves out of the box. Drop your axios instance / API client here.
 *
 * @example
 * import axios from "axios";
 *
 * export const api = axios.create({
 *   baseURL: "https://api.example.com",
 *   timeout: 15000,
 * });
 */

export {};
